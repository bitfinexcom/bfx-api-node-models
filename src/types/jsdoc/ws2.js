'use strict'

/**
 * WSv2 client from `bitfinex-api-node`
 *
 * @typedef {object} WSv2
 * @property {Function} cancelOrder - handler to cancel an order
 * @property {Function} submitOrder - handler to submit an order
 */
module.exports = {}
