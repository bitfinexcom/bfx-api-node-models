'use strict'

/**
 * A set of parameters describing an atomic {@link FundingOffer} that can be
 * sent via the WSv2 API.
 *
 * @typedef {object} FundingOffer~SubmitPayload
 * @property {string} type - type
 * @property {string} symbol - symbol
 * @property {string} amount - amount
 * @property {string} rate - rate
 * @property {number} period - period
 * @property {number} flags - flags
 */
