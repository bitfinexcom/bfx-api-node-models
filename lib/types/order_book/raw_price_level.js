'use strict'

/**
 * A raw {@link OrderBook} price level in object format.
 *
 * @typedef {object} OrderBook~RawPriceLevel
 * @property {number} price - price
 * @property {number} amount - amount
 * @property {number} orderID - order ID
 */
