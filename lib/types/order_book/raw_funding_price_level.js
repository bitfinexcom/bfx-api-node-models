'use strict'

/**
 * A raw order book **funding** price level in object format.
 *
 * @typedef {object} module:bfx-api-node-models.OrderBook~RawFundingPriceLevel
 * @property {number} rate - rate
 * @property {number} period - period
 * @property {number} amount - amount
 * @property {number} orderID - order ID
 */
