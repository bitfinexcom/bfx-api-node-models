'use strict'

/**
 * A raw {@link OrderBook} **funding** price level in object format.
 *
 * @typedef {object} OrderBook~RawFundingPriceLevel
 * @property {number} rate - rate
 * @property {number} period - period
 * @property {number} amount - amount
 * @property {number} orderID - order ID
 */
