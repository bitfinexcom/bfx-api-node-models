'use strict'

/**
 * An aggregate (non-raw) order book **funding** ticker price level in object
 * format.
 *
 * @typedef {object} module:bfx-api-node-models.OrderBook~AggregatedFundingPriceLevel
 * @property {number} rate - rate
 * @property {number} period - period
 * @property {number} amount - amount
 * @property {number} count - count
 */
