'use strict'

/**
 * An aggregate (non-raw) {@link OrderBook} **funding** ticker price level in
 * object format.
 *
 * @typedef {object} OrderBook~AggregatedFundingPriceLevel
 * @property {number} rate - rate
 * @property {number} period - period
 * @property {number} amount - amount
 * @property {number} count - count
 */
