'use strict'

/**
 * An aggregate (non-raw) order book price level in object format.
 *
 * @typedef {object} module:bfx-api-node-models.OrderBook~AggregatedPriceLevel
 * @property {number} price - price
 * @property {number} amount - amount
 * @property {number} count - count
 */
