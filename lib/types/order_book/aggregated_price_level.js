'use strict'

/**
 * An aggregate (non-raw) {@link OrderBook} price level in object format.
 *
 * @typedef {object} OrderBook~AggregatedPriceLevel
 * @property {number} price - price
 * @property {number} amount - amount
 * @property {number} count - count
 */
