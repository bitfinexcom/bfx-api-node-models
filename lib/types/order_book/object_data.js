'use strict'

/**
 * {@link OrderBook} data as an array of price levels in object format.
 * Suitable for passing to {@link OrderBook} to construct a model instance.
 *
 * @typedef {OrderBook~ObjectPriceLevel[]} OrderBook~ObjectData
 */
