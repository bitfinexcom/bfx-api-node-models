'use strict'

/**
 * Valid atomic order types, see the
 * {@link module:bfx-api-node-models.Order|Order} model.
 *
 * @typedef {(
 *   'MARKET'|
 *   'EXCHANGE MARKET'|
 *   'LIMIT'|
 *   'EXCHANGE LIMIT'|
 *   'STOP'|
 *   'EXCHANGE STOP'|
 *   'TRAILING STOP'|
 *   'EXCHANGE TRAILING STOP'|
 *   'FOK'|
 *   'EXCHANGE FOK'|
 *   'STOP LIMIT'|
 *   'EXCHANGE STOP LIMIT'
 * )} module:bfx-api-node-models.Order~Type
 */
