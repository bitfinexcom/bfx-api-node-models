'use strict'

/**
 * Valid atomic order types, see the {@link Order} model.
 *
 * @typedef {(
 *   'MARKET'|
 *   'EXCHANGE MARKET'|
 *   'LIMIT'|
 *   'EXCHANGE LIMIT'|
 *   'STOP'|
 *   'EXCHANGE STOP'|
 *   'TRAILING STOP'|
 *   'EXCHANGE TRAILING STOP'|
 *   'FOK'|
 *   'EXCHANGE FOK'|
 *   'STOP LIMIT'|
 *   'EXCHANGE STOP LIMIT'
 * )} Order~Type
 */
