'use strict'

/**
 * {@link Alert} data in plain object format. Suitable for passing to
 * {@link Alert} to construct a model instance.
 *
 * @typedef {object} Alert~ObjectData
 * @property {string} key - alert key
 * @property {string} type - alert type
 * @property {string} symbol - configured symbol
 * @property {string} price - configured price
 */
