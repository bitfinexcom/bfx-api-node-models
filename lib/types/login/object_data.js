'use strict'

/**
 * {@link Login} data in plain object format. Suitable for passing to
 * {@link Login} to construct a model instance.
 *
 * @typedef {object} Login~ObjectData
 * @property {number} id - id
 * @property {number} time - timestamp
 * @property {string} ip - client IP address
 * @property {object} extraData - metadata
 */
