'use strict'

/**
 * {@link UserInfo} data in WSv2 array format. Suitable for passing to
 * {@link UserInfo} to construct a model instance.
 *
 * @typedef {Array} UserInfo~ArrayData
 * @property {number} 0 - id
 * @property {string} 1 - email
 * @property {string} 2 - username
 * @property {number} 7 - timezone
 */
