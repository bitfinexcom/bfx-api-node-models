'use strict'

/**
 * {@link UserInfo} data in plain object format. Suitable for passing to
 * {@link UserInfo} to construct a model instance.
 *
 * @typedef {object} UserInfo~ObjectData
 * @property {number} id - id
 * @property {string} email - email
 * @property {string} username - username
 * @property {number} timezone - timezone as UTC offset
 */
