'use strict'

/**
 * {@link BalanceInfo} data in WSv2 array format. Suitable for passing to
 * {@link BalanceInfo} to construct a model instance.
 *
 * @typedef {Array} BalanceInfo~ArrayData
 * @property {number} 0 - amount
 * @property {number} 1 - amountNet
 */
