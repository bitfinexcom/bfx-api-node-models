'use strict'

/**
 * {@link BalanceInfo} data in plain object format. Suitable for passing to
 * {@link BalanceInfo} to construct a model instance.
 *
 * @typedef {object} BalanceInfo~ObjectData
 * @property {number} amount - total balance
 * @property {number} amountNet - net balance
 */
