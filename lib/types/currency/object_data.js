'use strict'

/**
 * {@link Currency} data in plain object format. Suitable for passing to
 * {@link Currency} to construct a model instance.
 *
 * @typedef {object} Currency~ObjectData
 * @property {string} id - id
 * @property {string} name - currency name ('Ethereum')
 * @property {string} pool - pool
 * @property {string} exporer - explorer URL
 * @property {string} symbol - symbol ('ETH')
 */
